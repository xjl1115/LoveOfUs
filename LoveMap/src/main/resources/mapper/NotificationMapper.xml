<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lovemap.mapper.NotificationMapper">

    <resultMap id="notificationMap" type="com.example.lovemap.model.entity.Notification">
        <id property="id" column="id"/>
        <result property="text" column="text"/>
        <result property="userId" column="user_id"/>
        <result property="isRead" column="is_read"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notification (text, user_id, is_read, created_at)
        VALUES (#{text}, #{userId}, #{isRead}, #{createdAt})
    </insert>

    <select id="selectByUserId" resultMap="notificationMap">
        SELECT id, text, user_id, is_read, created_at FROM notification
        WHERE user_id = #{userId}
        <if test="isRead != null">
            AND is_read = #{isRead}
        </if>
        ORDER BY created_at DESC
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*) FROM notification
        WHERE user_id = #{userId}
        <if test="isRead != null">
            AND is_read = #{isRead}
        </if>
    </select>

    <select id="countUnreadByUserId" resultType="long">
        SELECT COUNT(*) FROM notification
        WHERE user_id = #{userId} AND is_read = 0
    </select>

    <update id="markAsRead">
        UPDATE notification
        SET is_read = 1
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <update id="markAllAsRead">
        UPDATE notification
        SET is_read = 1
        WHERE user_id = #{userId} AND is_read = 0
    </update>

    <delete id="deleteById">
        DELETE FROM notification
        WHERE id = #{id} AND user_id = #{userId}
    </delete>

    <delete id="deleteReadByUserId">
        DELETE FROM notification
        WHERE user_id = #{userId} AND is_read = 1
    </delete>

    <select id="selectByIdAndUserId" resultMap="notificationMap">
        SELECT id, text, user_id, is_read, created_at FROM notification
        WHERE id = #{id} AND user_id = #{userId}
    </select>

</mapper>
